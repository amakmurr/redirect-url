{
  "name": "redirect-url",
  "version": "1.0.0",
  "description": "Simple Express app for image redirect with healthcheck",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "test": "node --test"
  },
  "dependencies": {
    "express": "^4.19.2"
  },
  "engines": {
    "node": ">=18"
  }
}
